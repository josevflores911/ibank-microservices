spring:
  application:
    name: transacoes
  h2:
    console:
      enabled: true
      path: /h2-console
  #  datasource:
  #    url: jdbc:postgresql://localhost:5555/icomprasprodutos
  #    username: postgres
  #    password: postgres
  #    driver-class-name: org.postgresql.Driver
  #  jpa:
  #    database-platform: org.hibernate.dialect.PostgreSQLDialect
  #    hibernate:
  #      ddl-auto: none
  #    show-sql: true
  #    properties:
  #      hibernate:
  #        format_sql: true

  kafka:
    bootstrap-servers: 'http://localhost:29092'
    consumer:
      group-id: ibank-atualizacao-pedido
      auto-offset-reset: earliest

server:
  port: 8084



springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

ibank:
  transacoes:
    clients:
      contas:
        url: 'http://localhost:8082/contas'
      clientes:
        url: 'http://localhost:8081/clientes'
      investimentos:
        url: 'http://localhost:8083/investimentos'
  config:
    kafka:
      server-url: 'http://localhost:29092'
      topics:
        pedidos-pagos: 'ibank.pedidos-pagos'
        pedidos-faturados: 'ibank.pedidos-faturados'
        pedidos-enviados: 'ibank.pedidos-enviados'
        logs: 'ibank.logs'
